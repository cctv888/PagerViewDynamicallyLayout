# 支持的iOS系统的最低版本
platform:ios,'10.0'

# 指定开源库的下载源（组件化开发时会用到）
#source 'https://github.com/CocoaPods/Specs.git'
#source 'https://code.houbank.net/xxx/xxxSpecs.git'

# ignore all warnings from all pods
inhibit_all_warnings!

# 使用framework（包含swift库就必须使用此设置，纯OC库可以不设置）
use_frameworks!

# 项目中如果有多个工程文件所以必须显式告知Cocopods，Pod的库属于哪个工程的依赖程的依赖
#project 'ProjectName.xcodeproj'

target 'PagerViewDynamicallyLayout' do

  pod 'Kingfisher'
  pod 'FSPagerView' ,:git => 'https://github.com/cctv888/FSPagerView.git'
  pod 'Cartography'

end
